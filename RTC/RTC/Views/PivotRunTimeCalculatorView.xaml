<UserControl
    x:Class="RTC.Views.PivotRunTimeCalculatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RTC.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="600">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75" />
            <RowDefinition Height="75" />
            <RowDefinition Height="75" />
            <RowDefinition Height="75" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="Headline" Text="Berechnen Sie Ihre Laufzeit" Margin="10" Style="{StaticResource SubheaderTextStyle}" Foreground="White" />
        <TextBox Grid.Row="1"  x:Name="TimeStamp" Width="300" Height="75" Margin="5" HorizontalAlignment="Left"/>
        <TextBox Grid.Row="2"  x:Name="Distance" Width="300" Height="75" Margin="5" HorizontalAlignment="Left"/>
        <Button Grid.Row="3" x:Name="Calculate" Content="Berechnen" FontSize="36.667"/>
    </Grid>
</UserControl>
